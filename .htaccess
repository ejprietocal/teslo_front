<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirige todo a index.html excepto archivos y carpetas reales
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^.*$ /index.html [L]
</IfModule>
